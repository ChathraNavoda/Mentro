import 'package:flutter/material.dart';

class TinyBoostVideoTask extends StatelessWidget {
  const TinyBoostVideoTask({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
